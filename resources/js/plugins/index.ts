// Side-effect imports registering client-side plugin code (PluginRegion
// components). The backend half of each plugin lives in src/Plugins/ and
// controls whether its UI entry points (tabs, nav) appear at all.

// Reference plugin — disabled to keep the demo out of the UI. Re-enable
// together with ExamplePlugin::class in config/platform.php.
// import './example';

export {};
